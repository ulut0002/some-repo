I need you to showcase a dashboard example. 

I want to use React-Grid-Layout  package because it fits to my needs. 

Create 5 example components separately. We will use them as widgets. 
Examples Widgets:

1. Display a spinning icon. (install Lucide icons for that). Make sure the color is assigned randomly when it is mounted.
2. Display an example table. 5 rows maybe? Assign values randomly. It
3. Display a random number of bars, like a chart with random values. MAximum 5 bars.
4. You choose the sample widget and develop it
5. You choose the sample widget and develop it. 

Also create a sample initial layout seed. It can have, let's say, 8 widgets on a dashboard. I want to demonstrate that the dashboard can be loaded at initially. 

Each item will be resizable on the dashboard. Make sure you utilize the package library, cause it saves values in local storage. 

You can view the showcase here: https://react-grid-layout.github.io/react-grid-layout/examples/00-showcase.html

I need each widget container to have a delete button. When user taps on it, it will confirm with user whether they want to remove it from the dashboard. And then it will delete it from the dashboard. I also want to have an add button on top right corner, to add a new widget to the system. IT will display existing widgets, and when user adds it, it will add it to the dashboard. This means we will have add and remove functionality, and then users can resize and move things around. 

